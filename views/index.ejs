<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Lightblub</title>
  </head>
  <body>
    <h1>Lightbulb - your local not-ready-for-use bot</h1>
    <p>Hi, I'm a dumbass.</p>
    <hr />
    <nav>
      <% if (req.cookies.qid) { %> <%=
      client.users.cache.get(req.cookies.qid).tag %>
      <a href="/logout">Logout</a> <% } else { %> <a href="/login">Login</a> <%
      } %>
    </nav>
  </body>
</html>
